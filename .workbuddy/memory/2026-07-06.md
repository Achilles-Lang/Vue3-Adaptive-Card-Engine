# 2026-07-06 项目开发日志

## 完成事项

### Sprint 0-1：项目骨架初始化
- 创建 `.workbuddy-rules.md` 全局规则文件
- 初始化 pnpm workspace：`pnpm-workspace.yaml` + `packages/core` + `examples/basic`
- 配置 TypeScript 6.0 兼容（baseUrl 弃用警告处理）
- 安装 vue/typescript/vite/vitest/vite-plugin-dts 等公共依赖
- 环境适配：系统全局 pnpm(corepack) 签名验证失败，改用 managed node 22.12.0 + 本地 npm 安装 pnpm

### Sprint 2：核心引擎实现
- `types.ts`：CardMessage 泛型接口 + 4 个内置数据接口 + isBuiltinType 类型守卫
- `registry.ts`：registerCard/registerCards/getCardComponent/resolveCardProps/clearRegistry
- `CardEngine.vue`：`<component :is>` + `v-for` + `v-bind` 核心 5 行渲染
- `FallbackCard.vue`：兜底组件，BEM 命名 ace-fallback-card
- 包入口 `index.ts`：统一具名导出

### Sprint 3：业务卡片
- TextCard / TodoCard（复选框交互）/ ProgressCard（渐变进度条）/ ChartCard（纯 div 柱状图）

### Sprint 4：示例联调
- main.ts 使用正式包名 `vue3-adaptive-card-engine` 批量注册
- App.vue 4 条模拟数据 + CardEngine 渲染
- vite.config.ts 配置源码别名联调
- Dev server 启动成功，`http://localhost:5173/`

### Sprint 5：构建 + 测试
- Library 模式构建：es + umd + d.ts，output 到 `packages/core/dist/`
- 10 个单元测试全部通过（registry 7 + engine 3）
- jsdom 版本适配：v29 不兼容，降级至 v24

### Sprint 6：CI/CD + 文档
- `.github/workflows/ci.yml`：type-check → test → build
- `.github/workflows/release.yml`：v* tag → npm publish
- `scripts/sync-template.sh`：双分支模板同步脚本
- `README.md`：Mermaid 架构图 + 快速开始 + API 文档

## 环境注意事项
- pnpm 使用：必须清除 NODE_OPTIONS，使用 managed node 22.12.0 + 本地 pnpm.cjs
- Windows 下 .cmd/.sh 混用问题：PowerShell 跑 .cmd，bash 跑 .sh
- npm install 需要 `--legacy-peer-deps` 标志（vite 8 + plugin-vue 版本冲突）
- jsdom 需用 v24 而非 v29（后者与 Node 20.17 不兼容）

## 下一步
- ~~开发者手动验证 Sprint 4（浏览器检查 4 种卡片渲染）~~ ✅ 已完成
- ~~初始化 Git 仓库，推送到 GitHub~~ ✅ 已完成
- ~~template 分支已创建~~ ✅ 已完成
- 设置 GitHub Actions 秘钥 NPM_TOKEN（可选，NPM 发布前）
- ~~CI 修复~~ ✅ 已完成

### GitHub 推送记录（18:10-18:20）
- git init + 首次 commit（37 files）
- 远程仓库：Achilles-Lang/Vue3-Adaptive-Card-Engine
- Fine-grained PAT：Contents:Write + Workflows:Write + Metadata:Read
- main 分支已推送，v1.0.0 tag 已推送，template 分支已创建
- CI 修复记录：
  - fix 1: pnpm-lock.yaml 过期 → 重新生成（npm 装的 devDeps 未同步到 pnpm）
  - fix 2: examples/basic 无测试文件导致 `pnpm -r test` 失败 → 移除其 test 脚本
  - fix 3: jsdom v29 不兼容 Node 20 → root package.json 改为 ^24.1.0
